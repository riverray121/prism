# Dev Log

## Completed

- Design phase: full design docs written — `design-doc.md`, `feature-catalog.md`, `profile-schema.md`, `build-order.md`, `development.md`.
- GitHub repo created: `riverray121/prism` (public), `origin` tracking `main`.
- **M0** — Tauri scaffold: Svelte 5 + SvelteKit (static SPA) + Tailwind v4 frontend, Rust shell. Frontend builds, Rust shell compiles. Demo page replaced with minimal placeholder.
- **M0** — Python sidecar: uv project (Python 3.12), `sidecar/` package with pydantic IPC models. `python -m sidecar` reads JSON-lines on stdin, echoes `pong`; logs to stderr to keep stdout clean.
- **M0** — Rust shell spawns sidecar (`uv run python -m sidecar`), forwards its stdout lines to the frontend as `sidecar-message` events, exposes `send_to_sidecar` command. Frontend sends ping, displays pong. End-to-end ping/pong round-trip (manually verified).
- **M0** — Tooling: pre-commit with ruff (Python) and Prettier + Svelte/Tailwind plugins (frontend); root README. **M0 complete.**
- **M1 slice 1 — SQLite + import.** `library.db` (`songs` table, full design-doc column set), per-song folder `library/songs/{uuid}/source.ext`. Metadata via mutagen (tags → `Artist - Title` filename → "Unknown"; duration/sample-rate across all 4 formats). IPC: `library.import` / `library.list` → `library.songs` snapshot. Frontend: `$lib/ipc` (zod-validated event stream), `$lib/state`, `LibraryPanel` with native file picker (tauri-plugin-dialog). Verified end-to-end in the running app with real test tracks (FLAC + MP3).
- **M1 slice 2 — Worker + BPM.** Background worker thread (`worker.py`) drains the `status='queued'` set oldest-first, computes BPM (librosa, `features/rhythm.py`), writes `profile.json` (`mix.bpm` scalar), flips `queued → analyzing → analyzed` (or `failed`). `queue.add` command; status live-updates via `library.songs` snapshots emitted on each transition (`ipc.py` serializes stdout writes across threads). Frontend: Analyze/Retry button per row. Verified end-to-end in the running app (BPM 161.5 for Joji – Dior).
- **M1 slice 3 — Inspection view (text).** `profile.get` command → `profile` event (sidecar reads `profile.json`; frontend owns no disk access). Frontend: zod `Profile` schema, `$lib/state/inspection`, `InspectionView` component, clickable analyzed rows, library/inspection view toggle. Shows BPM as text. Verified in the running app.
- **M1 slice 4 — RMS line graph.** Profile gains `timeline` (frame_rate_hz, frame_count) and a continuous `rms` feature (peak-normalized 0–1, ~100 Hz). Worker now loads the signal once and passes `(y, sr)` to pure extractors (`rhythm.compute_bpm`, `amplitude.compute_rms`). Frontend: `ContinuousFeature` zod schema, `RmsGraph` component (concrete uPlot line, x=seconds from frame index, responsive via ResizeObserver). Verified in the running app.
- **M1 slice 5 — Playback + playhead. M1 COMPLETE.** Asset protocol enabled (`protocol-asset` feature + `assetProtocol` scope `$HOME/**`); `profile.get` reply carries the absolute `audio_path` (runtime-only, kept out of profile.json). Playback uses the **Web Audio API**: the file is fetched via `convertFileSrc`, decoded to an `AudioBuffer`, played through an `AudioBufferSourceNode`; the playhead is derived from `AudioContext.currentTime` (the same clock generating the audio) so the two can't drift, and seeking restarts the source at an exact offset. Drawn as a vertical line in `RmsGraph` via a uPlot draw hook. Verified end-to-end: audio plays, playhead sweeps in sync, scrubbing lands sample-accurate.
- **M2 slice 1 — continuous DSP batch + pipeline generalization.** `mix` is now a keyed map of feature envelopes end-to-end: `storage.write_profile` takes the assembled `mix` dict; the worker builds it from per-feature extractors and aligns every continuous feature to a shared `frame_count` (truncate to shortest — librosa lengths can differ by a frame from centering). New extractors off the single loaded mono signal + one shared magnitude spectrogram: `frequency.py` (spectral centroid/rolloff/flatness/flux, 6 band energies as fraction-of-total-energy), `amplitude.peak`, `amplitude.dynamic_range` (crest factor dB), `timbre.zero_crossing_rate`, `tonal.key`/`key_confidence` (Krumhansl-Schmuckler chroma correlation), `tonal.tuning_deviation`. Frontend: `mix` zod schema → `z.record` of a render-discriminated `MixFeature` union; scalar `value` now `number | string` (for `key`); `RmsGraph` → generalized `ContinuousGraph` (prop `feature`, label/color/height); `InspectionView` splits the map into a scalar grid + stacked continuous graphs (shared playhead/scrub, per-graph wheel-zoom, double-click resets zoom). Verified extractor alignment/ranges on a synthetic signal; frontend `pnpm check` clean.
- **M2 slice 2 — `beats` (first `event` feature).** `rhythm.rhythm_features` does one `beat_track` pass producing both `bpm` (scalar) and `beats` (event, list of `{t}`); worker spreads it into the mix. Frontend: `EventFeature` zod schema added to the `MixFeature` union (events `passthrough` extra attrs for future chords); new `EventGraph.svelte` renders vertical ticks via a uPlot draw hook over a hidden series, mirroring `ContinuousGraph`'s playhead/scrub/zoom; `InspectionView` renders event features as tick lanes above the continuous graphs. Verified beats align to onsets (0–23ms median across 3 tracks). Known: at full-song zoom a few-hundred ticks render as an unresolvable ~1px picket fence; readable when zoomed in.
- **M2 slice 3 — heatmap `.npy` sidecar pipeline (first heatmap, `mfcc`).** First feature whose payload lives in a file, not inline in the profile. `timbre.mfcc` returns a `[13, frames]` matrix; the worker truncates it to `frame_count`, writes `heatmaps/mfcc.npy` (uncompressed float32) via `storage.write_heatmap`, and registers a heatmap envelope (`sidecar`/`shape`/`axes`) in the mix. `ProfileEvent` gained `song_dir` (absolute) so the frontend can resolve relative sidecar paths against the asset protocol. Frontend: `lib/npy.ts` parses the `.npy` subset we write (C-order little-endian f4); `HeatmapFeature` zod schema in the `MixFeature` union; new `HeatmapGraph.svelte` fetches+parses the sidecar, builds a colormapped offscreen canvas (per-row normalized — MFCC's energy-dominated row 0 would otherwise flatten the rest; verified against `librosa.display.specshow`), and blits the visible time-slice in a uPlot draw hook reusing the shared playhead/scrub/zoom. Third concrete graph type → the generic graph abstraction is now due (slice 4). Note: matplotlib is not a project dep; reference plots are generated ad hoc via `uv run --with matplotlib`.
- **M2 slice 4b — `spectrogram` heatmap; M2 COMPLETE.** `frequency.spectrogram` = log-power dB spectrogram (linear freq, n_fft=1024 → 513 bins, `ref=np.max` so values ≤ 0), written as a `.npy` heatmap. `HeatmapGraph` gained a `normalize` mode: `global` (one range for the whole matrix — correct for spectrogram dB, preserves loudness across frequencies) vs `per-row` (mfcc/chroma). 24 mix-level features total. All remaining catalog features reassigned to M3–M6 — see `build-order.md`.
- **M2 slice 4a — `chroma`, `loudness_lufs`, `stereo_width`.** No frontend changes — the generalized pipeline renders them via the existing generic components (`chroma` heatmap, the other two continuous). `tonal.chroma` (chroma_cqt, 12×frames heatmap); `amplitude.loudness_lufs` (pyloudnorm K-weighting, 400ms momentary window per frame, floored at the EBU R128 −70 LUFS gate so silence doesn't dominate the scale); `spatial.stereo_width` (per-frame mid/side energy ratio, 0 mono → ~1 wide). Worker now loads stereo once and derives mono (`librosa.to_mono`) so spatial features get channels. `storage.write_heatmap` forces C-order (`np.ascontiguousarray`) — chroma_cqt returns Fortran-order arrays and the frontend .npy parser only reads C-order. Added `pyloudnorm` dep.
- **M1 polish — graph interaction.** Drag/click on the RMS graph scrubs the playhead (silenced during drag, resumes on release to avoid source-restart popping). Zoom centered on cursor: trackpad pinch or mouse wheel; trackpad two-finger swipe pans the zoomed window; vertical trackpad scroll no longer zooms (gesture routing via `ctrlKey`/`deltaMode`/`deltaX`). Reset zoom button; Follow toggle (Center keeps the playhead centered while playing, Page lets it sweep and jumps a window at the edge). Graph enlarged (height 400, inspection view widened to max-w-4xl).
- **M3 slice 1 — `chords` (BTC transformer); torch enters.** Added `torch` (2.12, macOS arm64 wheel — MPS verified, serves Demucs in M4). Vendored BTC model code under `sidecar/vendor/btc/` (`btc_model.py`, `transformer_modules.py`, MIT, attribution in `__init__.py`; edits: relative imports, dropped `__main__`/`HParams`, `np.float`→`float` for numpy 2.x). `sidecar/models.py`: download-on-first-run registry (checksum-verified, cache in `~/Library/Application Support/Prism/models/`, `PRISM_MODELS_DIR` override); BTC checkpoint mirrored to the `models-v1` GitHub Release on our repo. `sidecar/features/chords.py` replicates BTC inference on the in-memory signal (blocked CQT → log-mag → checkpoint mean/std norm → 108-frame Transformer blocks → per-frame argmax + softmax confidence → run-length-encoded `{t, root, quality, confidence}` events). Wired into the worker mix pass; runs on CPU (~0.6s for a 4.7-min track). Renders via the existing `EventGraph` tick lane (`EventSchema` already passes extra attrs through). Frontend: `EventGraph` gained an optional `labelFor` prop drawing per-event text in a reserved top band with overlap-dropping (beats pass none → unchanged); `InspectionView` passes a `chordLabel` formatter (compact musical shorthand, maj→bare root, `ø7` for hdim7) for the chords lane only. Verified end-to-end in the running app: 177 coherent chord events render as a tick lane with labels, continuous alignment intact, sidecar boots cleanly with torch.
- **M4 slice 1 — multi-stage worker + single-engine separation + per-stem DSP.** First long-running stage and the first fan-out of the pipeline into stages. `audio-separator` (0.44.2) + `onnxruntime` (CoreML EP) added; de-risked by running `htdemucs_ft` (Demucs on MPS, ~90s for a 2-min track) before any integration. `sidecar/separation.py` wraps audio-separator behind a thin `separate(source, out_dir, engine)` (engine set is config — `DEFAULT_ENGINES`; slice 1 = `["htdemucs_ft"]`), normalizing outputs to canonical `stems/{engine}/{stem}.wav` and pointing the model cache at the M3 `models.MODELS_DIR`; stdout is redirected to stderr during separation to keep the JSON IPC channel clean. `sidecar/features/stem.py` runs a reused-extractor pass per stem (energy←rms, spectral_centroid, mfcc heatmap), aligned to the song's `frame_count`. The worker (`worker.py`) is now staged `dsp-mix → separate → dsp-stem`, writing `current_stage`/`current_stage_progress` to the song row at each boundary (snapshot-driven progress, not a `job.*` stream — design-doc reconciled). `storage.write_profile` gained a `stems` block; `write_heatmap` already nests via subdir-in-name (`heatmaps/{engine}/{stem}_mfcc.npy`); `cleanup_partial` drops partial separation output on failure. Robustness: startup marks any stuck `analyzing` row `failed` ('Analysis interrupted'). Snapshot plumbing: `Song` schema + `list_songs` carry the stage fields; frontend `Song`/`Profile` zod schemas extended (`stems` defaults `{}` for pre-M4 profiles), `LibraryPanel` shows `stage NN%` while analyzing, `InspectionView` renders a per-engine Stems section reusing the existing graph components (shared playhead/scrub). Verified: isolated `stem_features` alignment/range checks on a synthetic signal, ruff + `pnpm check` clean, one real separation run. **Not yet run end-to-end through the app by the user.** Queued-song cancellation, extra engines (bs_roformer/mel_band_roformer/ensemble), and the rest of the per-stem catalog (onsets/transients/pitch/vibrato) are later M4 slices.
- **M4 slice 2 — full single-model engine set + `current_engine` snapshot.** Engine set expanded from one to three: `htdemucs_ft` (vocals/drums/bass/other) + `bs_roformer` (`model_bs_roformer_ep_317_sdr_12.9755.ckpt`) + `mel_band_roformer` (`model_mel_band_roformer_ep_3005_sdr_11.4360.ckpt`), the last two emitting vocals/instrumental and running on torch/MPS (audio-separator MDXC arch — no ONNX needed; de-risked at ~93s each + ~28s model load). The slice-1 `separation.separate()` wrapper handled both new engines unchanged (filename `_(Stem)_` parsing already canonicalizes). Snapshot gained `current_engine`: added via a non-destructive `ALTER TABLE ADD COLUMN` (`_ADDED_COLUMNS` in `library.connect`, no DB wipe); `mark_stage` carries the engine; `list_songs`/`schema.Song`/frontend `Song` zod thread it through; `LibraryPanel` shows `stage · engine NN%`. Worker sets `current_engine` across the `separate`/`dsp-stem` stages (progress = engine fraction). `InspectionView` stems section regrouped under one **Stems** heading with per-engine subheadings so the same stem is comparable across engines. Verified: migration + `mark_stage` engine round-trip on a temp DB, ruff + `pnpm check` clean. Ensemble (vocal_balanced preset) deferred to a fast-follow — different code path + 2 extra heavy checkpoints. **Not yet run end-to-end by the user; existing slice-1 profiles keep their single engine until re-analyzed.**
- **M4 slice 4 — queued-song cancellation.** `queue.cancel {song_id}` → `library.cancel_queued` sets a queued row back to `unanalyzed` (guarded to `status='queued'`, so a song the worker already claimed keeps running — running-song cancel stays v2, and the guard also resolves the queued→analyzing claim race). `QueueCancelCommand` schema + `__main__` handler emit a fresh snapshot; frontend `cancelAnalysis` sender + a **Cancel** button on queued rows in `LibraryPanel`. Verified the guard on a temp DB (queued→unanalyzed, analyzing untouched); ruff + `pnpm check` clean. **Not yet run end-to-end by the user.**
- **M4 slice 3 — per-stem feature catalog.** Each stem now gets `onsets` (event, `librosa.onset`, time + peak-normalized strength) and `transient_sharpness` (continuous, onset-strength envelope 0-1); non-percussion stems add `pitch` + `pitch_confidence` (`librosa.pyin`, f0 Hz + voicing prob, C2–C7); vocals add `vibrato_rate`/`vibrato_depth` (v1 proxy — detrend the pitch contour ~0.25 s, then windowed zero-crossing-rate → Hz and RMS → cents). New `features/pitch.py`; `onsets` in `rhythm.py`, `transient_sharpness` in `timbre.py`; `stem.stem_features` gained `stem_name` and branches by stem type (drums → no pitch; vocals → +vibrato); `scipy` declared explicitly (`uniform_filter1d`). Frontend: stems section renders event features (onsets tick lane via `EventGraph`); new continuous features render through the existing `ContinuousGraph`. Verified extractors on synthetic signals (6/6 onsets, pitch 220 Hz @ 0.94 conf, vibrato ~5 Hz/~28 cents) + stem-type wiring; ruff + `pnpm check` clean. **Not yet run end-to-end by the user.** pyin adds analysis time per melodic stem.
- **M4 — `htdemucs_6s` (6-stem) engine added; ensemble dropped.** Added Demucs `htdemucs_6s` to `ENGINES` (vocals/drums/bass/guitar/piano/other) — de-risked on a 25s clip (6 stems, filenames parse via the existing `_(Stem)_` regex, ~7s). One-line config add; the wider stem set flows through the per-stem DSP + UI unchanged; friendly label added in `AnalysisSettings`. Ensemble (`vocal_balanced`) dropped from M4 to `ideas.md`: the two-model preset overcommits the 18 GiB dev machine's unified memory and thrashes swap. Verified ruff + `pnpm check` clean. **Not yet run end-to-end by the user.**
- **M4 slice 2 polish — step-counter progress + analysis-settings panel.** Progress switched from a frozen percentage (audio-separator exposes no intra-step progress) to a discrete engine step counter: snapshot carries `current_step`/`total_steps` (engine k of N) replacing `current_stage_progress`; `LibraryPanel` shows e.g. `separating bs_roformer (2/3)` / `stem features … (2/3)` / `analyzing mix`. Added a global **Analysis settings** panel on the main page: a key-value `settings` table in `library.db` (`get_setting`/`set_setting`), `settings.get`/`settings.update` IPC commands and a `settings` event (selected + available engines), emitted on connect and after each change. `library.get_engines` returns the selected set (defaults to all `separation.ENGINES`, validates/orders, empty = mix-only); the worker reads it per song instead of a hardcoded list (`DEFAULT_ENGINES` removed). Frontend: `settings` store, `AnalysisSettings.svelte` with a checkbox per engine (optimistic update + sidecar echo). Verified: settings round-trip + validation + step model on a temp DB, ruff + `pnpm check` clean.
- **M3 slice 2 — `downbeats` (librosa heuristic); M3 COMPLETE.** `rhythm._infer_downbeats` rides the existing `beat_track` pass: assumes 4/4, scores each of the 4 beat phases by onset strength + low-band (<150 Hz, kick) energy sampled at the beats (beat frames, onset env, and STFT all share librosa's 512 hop so indices align), and takes every 4th beat from the top phase. `rhythm_features` returns a third entry `downbeats` (event, `{t, bar}`); worker already spreads it into the mix. No frontend change — renders as a tick lane via `EventGraph`. Verified on a real track: 647 beats → 162 downbeats (ratio 3.99 ≈ 4/4), downbeats a subset of beats, spacing ≈ 4 beat-intervals. Locked to 4/4; non-4/4 meters wrong by design (upgrade paths noted above).

## Todo

- **M2 COMPLETE.** All cheap, dependency-light mix-level DSP is done (24 features across scalar/continuous/event/heatmap). Every remaining catalog feature is explicitly reassigned in `build-order.md` — clean exit. Highlights of what moved:
  - `chords` (BTC transformer) + `downbeats` → **M3** (with the torch dependency).
  - Demucs + per-stem DSP + multi-stage worker → **M4**.
  - `silence` (segment) + `rhythmic_density` → **M5** (with the `segment` render mode).
  - `swing`/`harmonic_complexity`/`reverb_amount`/`roughness` (`[WIP]`) → **M6**.
- **M3 COMPLETE.** `chords` (BTC transformer, torch) + `downbeats` (librosa heuristic) both done — see Completed. Torch is in and de-risked; model-weights storage settled (download-on-first-run). App freezing/packaging is out of development scope — see the packaging note below.
- **M4 — Multi-engine stem separation + per-stem DSP.** Slice 1 done (see Completed): multi-stage worker, single-engine (`htdemucs_ft`) separation, minimal per-stem DSP, snapshot stage/progress, startup interrupt sweep + partial cleanup, stems rendered in the UI. Remaining slices:
  - **Slice 2 done** (see Completed): three single-model engines + `current_engine` snapshot + per-engine UI grouping.
  - **Slice 3 done** (see Completed): per-stem catalog — onsets, transient_sharpness, pitch/pitch_confidence, vocals vibrato.
  - **Slice 4 done** (see Completed): queued-song cancellation (`queue.cancel`); running-song cancel stays v2.
  - **Slice 5 — drum sub-separation.** Two-stage: after an engine emits a `drums` stem, run a drum-separation model (`MDX23C-DrumSep-aufr33-jarredou.ckpt`) on it to split kick/snare/toms/hh/ride/crash. Needs the `Separator` interface to accept a stem WAV as input and write nested sub-stems (`stems/{engine}/drums/{kick,snare,…}.wav`); the schema gains a sub-stem level under any stem. Only runs for engines that produce a `drums` stem (Demucs).
- **M5 — ML classification + structure** (PANNs, sections/msaf, motifs, novelty) + deferred mix DSP (`silence`, `rhythmic_density`).
- **M6 — single UI/UX redesign + favorites/polish.** All UI design happens here, in one pass: the dashboard (shared time axis, shared zoom/playhead, Y-axis dropdown per feature) and consolidating the zoom/scrub/playhead code duplicated across `ContinuousGraph`/`EventGraph`/`HeatmapGraph` into one shared time-axis layer. No incremental dashboard or UI design before M6.

## Notes

- **Stem separation → multi-engine via `audio-separator`, not a single Demucs (M4).** Goal is max separation quality + side-by-side comparison across engines; speed is not a constraint. `audio-separator` ([nomadkaraoke/python-audio-separator](https://github.com/nomadkaraoke/python-audio-separator)) gives one interface over Demucs, MDX, MDX23C/MDXC, VR, and RoFormer (BS-/Mel-Band) plus built-in ensemble and auto model download. Verified vs. the points raised: BS-/Mel-Band RoFormer are SOTA and clearly beat Demucs on SDR (BS won SDX'23, ~12 dB vs Demucs ~9); the "Demucs is still best for drums" carve-out is the one outdated claim — modern RoFormers match/beat it on drum SDR, so it goes in the comparison rather than being assumed. RoFormer/MDX run on Apple Silicon via ONNX Runtime's CoreML provider; [`mlx-audio-separator`](https://github.com/ssmall256/mlx-audio-separator) (MLX-native, ~1.85× faster, near drop-in) is the fallback if CoreML/MPS is flaky. Wrapped behind a thin sidecar `Separator` interface so engines are swappable. **Schema impact:** `stems` gains an engine dimension — `stems.{engine}.{stem}` on disk (`stems/{engine}/`) and in `profile.json`; favorites dot-paths become `stems.{engine}.{stem}.features.{name}`. **Every engine's stems are kept on disk — no v1 cleanup** (storage grows ~N× with engine count; pruning deferred). Docs updated: `build-order.md` (M4), `design-doc.md`, `profile-schema.md`, `feature-catalog.md`.

- **Cold-start DB race fixed (`library.connect`).** Switching SQLite to WAL takes an exclusive lock and ignores `busy_timeout`, so on a freshly created `library.db` the worker thread and the UI's `library.list` could collide on the switch and one would crash with "database is locked" (worker thread died → no analysis). WAL is a persistent DB property, so `connect` now only switches if not already WAL and tolerates a concurrent setter. Surfaced after wiping the library to start fresh; latent since M2 (a warm DB never contends here).

- **Chords → BTC transformer in M3, not madmom/essentia.** Evaluated [jayg996/BTC-ISMIR19](https://github.com/jayg996/BTC-ISMIR19) (ISMIR'19 bi-directional Transformer, PyTorch). Chosen over madmom (install friction, non-commercial model license, maj/min only) and essentia (AGPL, template-based, finicky wheels): MIT license (code + weights), 12 MB checkpoint, CPU-fine, 170-chord large vocabulary, librosa-CQT input. Cost is the PyTorch dependency — so chords is its own milestone (M3), the first to pull in torch, kept ahead of Demucs (M4) to de-risk torch on a light model before the heavier stage. We vendor its model code (not a pip package); inference replicates its CQT params + checkpoint mean/std normalization + 10s/108-step blocking.
- **Downbeats use a librosa heuristic; improvable later.** Starting with the librosa-based heuristic for `downbeats`. If accuracy isn't good enough, there are better options to upgrade to later (e.g. madmom, a torch model).
- **Model weights → download-on-first-run, never in git.** Pretrained weights (BTC ~12 MB in M3, Demucs ~80–300 MB in M4, PANNs ~80–300 MB in M5) are large opaque binaries — committing them bloats history permanently (git keeps every copy forever, even after deletion) and GitHub hard-rejects files >100 MB. So: a `sidecar/models.py` registry (`{name, url, sha256, dest}`) downloads each model on first use into a gitignored app-data cache (`~/Library/Application Support/Prism/models/`), checksum-verifying so a truncated download fails loudly instead of corrupting analysis downstream. Only BTC needs this hand-rolled (vendored code, no pip auto-fetch); mirror its checkpoint to a GitHub Release on our repo (Releases allow 2 GB/asset, don't count against repo size) so the URL is ours, not a research repo that may rot. Demucs/PANNs auto-download their own weights — just point their cache at the same dir. Chosen over Git LFS: LFS suits team/CI repos that must lockstep-version weights with code; for a solo Mac app it's quota anxiety and setup cost for reproducibility we don't need.
- **All UI design deferred to one M6 redesign.** Through M2–M5 each feature renders into the existing minimal stacked-graph layout, just enough to confirm the data. No dashboard, no shared-axis/zoom consolidation, no layout polish until M6 — the whole UI is designed and built once, against a UI/UX design doc, after the full feature set exists. The duplicated zoom/scrub/playhead code across the three graph components is intentional debt to be paid off then.
- **Sidecar launch: spawn from source.** Tauri's Rust shell spawns `uv run python -m sidecar`; reproducibility comes from `uv.lock`. Chosen over a bundled binary so the edit-test loop stays fast.
- **Python pinned to 3.12** via uv (not system 3.13). De-risks madmom and essentia, which lag new Python releases and have M-series build friction.
- **No ORM for `library.db`.** Queries are hand-written stdlib `sqlite3`, all isolated in `library.py`; callers never see SQL. Fine while the DB is a single index table. **Shipping caveat:** today the DB is dev-only and gitignored, so a schema change just means recreating it — but if we ever ship to users, changing the schema means real migrations against databases they already have. At that point adopt a migration story (and likely SQLModel, since pydantic is already in use).
- **App packaging — distribution only, deferred; not a development task.** Bundling the sidecar into a standalone executable (PyInstaller/PyOxidizer → `src-tauri/binaries/`) so users without Python can run it. Hard part: torch/Demucs native libs need manual PyInstaller hooks. Open choice for then: keep download-on-first-run weights (smaller installer, needs network once) vs bundle weights into the app (offline, larger). Revisit only if we ship.
