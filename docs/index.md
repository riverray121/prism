# Docs index

Milestones in build order. Only the ACTIVE one is current.

| #   | Milestone                                           | Status   | Summary                                                                                                                                                                                                                                                                                                                                                                                                                              |
| --- | --------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | [analysis-pipeline](milestone-1-analysis-pipeline/) | shipped  | Full audio-analysis pipeline: import → multi-engine stem separation → mix + per-stem feature catalog (DSP + ML), rendered against synced playback. Reviewed and cleaned.                                                                                                                                                                                                                                                             |
| 2   | [ui-rework](milestone-2-ui-rework/)                 | shipped  | Frontend overhaul into a project-centric workspace: tabbed shell (Analysis live; Mapping/Sim/Hardware stubbed), split view, global transport, collapsible library sidebar, rebuilt modular graph kit at parity + onset lanes + favorites, full library (file/YouTube import, analyze popup, metadata, filters), Zed-inspired dark-first restyle. Built as 13 slices; user-tested. Repo-wide `/clean` deferred to the next milestone. |
| 3   | [test-suite](milestone-3-test-suite/)               | shipped  | Bootstrap the `pytest` + `vitest` suite specified in M1's practice doc but never built; plus the observability subfeature (crash detection + persistent logs, `observability/`). Unit-test every cleanly reachable pure unit: sidecar DSP extractors, schema/validation, storage + library state, and the frontend `.npy` parser + IPC schemas. Foundational, runs alongside M2.                                                     |
| 4   | [mapping](milestone-4-mapping/)                     | active   | Sound → light mapping on one generic fixture (all channels incl. an N-pixel dimension): saved derivations, fixture-agnostic programs with transform chains, live frontend evaluation, lit-ribbon preview lanes + mini live light preview, hand authoring from favorites (on-demand auto-map only), macro layer (scenes from sections + adaptive master). Rig/patch/Sim/Hardware and additional fixture types deferred.               |
| 5   | [hardware](milestone-5-hardware/)                   | active   | Hardware tab on real fixtures: mDNS discovery of ESP32/WLED hubs, auto-connect + Connect new, app-level rig (hubs + named lights, config read from the hub), per-song patch of mapping programs onto lights, Rust-shell DDP streamer synced to playback, strip off whenever not playing.                                                                                                                                             |
| 6   | rust-sampler                                        | planning | Move the hardware frame sampler from the webview render loop into the Rust shell: ship the precomputed show to Rust, mirror play/pause/seek/rate, sample and send on a shell-side clock. Removes the stall when the window is minimized during playback.                                                                                                                                                                             |

Status: `planning` → `active` → `shipped`.

Cross-milestone references (not milestone-scoped): [`feature-catalog.md`](feature-catalog.md) (every analyzed feature), [`profile-schema.md`](profile-schema.md) (profile.json spec), [`ideas.md`](ideas.md) (unscoped backlog).
