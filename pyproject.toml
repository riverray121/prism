[project]
name = "prism-sidecar"
version = "0.1.0"
description = "Prism analysis sidecar"
requires-python = ">=3.12,<3.13"
dependencies = [
    "librosa>=0.11.0",
    "mutagen>=1.47.0",
    "numpy>=2.4.6",
    "pydantic>=2",
    "pyloudnorm>=0.2.0",
]

[tool.uv]
package = false
